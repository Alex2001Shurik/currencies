package com.currencies.presentation.main.callback.activity

import com.currencies.presentation.main.ToolbarOptions

interface ParentUpdateToolbarCallback {
    fun updateToolbar(toolbarOptions: ToolbarOptions)
}
